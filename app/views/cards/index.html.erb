<div class="index-page bg-white">
  <div class="d-flex flex-column align-items-center justify-content-end">
    <div class="main-content">
      <div class="main-heading primary">
        <h1>Hi <%= current_user.first_name %>!</h1>
      </div>
    </div>
  </div>

  <div class="d-flex align-items-center justify-content-evenly">
    <%= render 'shared/searchbar' %>
    <div class="sliders">
      <%= link_to edit_category_path(1) do %>
        <i class="fa-solid fa-sliders"></i>
      <% end %>
    </div>
  </div>

  <div class="text-center">
    <div class="wish-button-container">
      <%= link_to new_card_path(search_keyword: @search_keyword), class: "wish-custom-button" do %>
        Wish for a new card!
      <% end %>
    </div>
  </div>

  <!--
  <div class="text-center mb-3" >
    <% if current_user.categories.present? %>
          Topics selected:
          <div class="category-posters d-flex justify-content-equally justify-content-center mb-3">
            <% current_user.categories.each do |category| %>
              <div class="category-tag">
                <%= category.tag %>
              </div>
            <% end %>
          </div>
      <% end %>
  </div>
    -->
  <!--   modal if we want to use it in the future
  <button class="wish-custom-button" data-modal-new-card-target="button" data-bs-toggle="modal" data-action="click->modal-new-card#fetch" data-bs-target="#newCard" data-href="<%= new_card_path %>">
    Wish for a new card!
  </button>

  <div class="modal fade" id="newCard" tabindex="-1" aria-labelledby="newCardLabel" aria-hidden="true" data-modal-new-card-target="modal">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-body" data-modal-new-card-target="modalContent">
          check this out!
        </div>
      </div>
    </div>
  </div>
  -->

  <div class="card-container">
    <%= render 'card', locals: { card: @card } %>
  </div>
</div>
