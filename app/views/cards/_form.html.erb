<%= simple_form_for(card) do |f| %>
    <%= f.input :primary_keywords, placeholder: "Any keywords in particular?" %>
    <%= f.input :secondary_keywords, placeholder: "Anything else you want to add?"  %>
    <%= f.button :submit, class: "btn btn-success" %>
  <% end %>
