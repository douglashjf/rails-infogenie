<%= simple_form_for(card) do |f| %>
    <%= f.input :content, placeholder: "content" %>
    <%#= f.input :keyword, placeholder: "Any keywords in particular?"  %>
    <%= f.button :submit, class: "btn btn-success" %>
  <% end %>
