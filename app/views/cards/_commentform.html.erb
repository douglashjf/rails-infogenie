<%= simple_form_for([card, comment], data: { insert_in_comment_target: "form", action: "submit->insert-in-comment#send" } )do |f| %>
    <%= f.input :content,
      input_html: { "data-target" => "insert-in-comment.hello", class: "comment"},
      label: false,  as: :text, placeholder: 'Write your comment...' %>

    <%= f.submit 'Add Comment', class:"btn btn-success button-container text-white " %>
<% end %>

<%# data: { insert_in_comment_target: "form", action: "submit->insert-in-comment#send" } %>
